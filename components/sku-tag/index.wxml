<view bind:tap="onTap" class="container {{status=='forbidden'?'forbidden':''}}">
    <view class="inner-container">
        <image src="{{image}}" mode="aspectFit" class="img" wx:if="{{image}}"></image>
        <text>{{text}}</text>
    </view>
</view>